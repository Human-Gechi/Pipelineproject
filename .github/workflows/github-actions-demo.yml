name: Daily News Job

on:
  schedule:
    - cron: "5 4 * * *"
  workflow_dispatch:

jobs:
  run-news-script:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run news script
        run: python scripts/load_to_db.py
