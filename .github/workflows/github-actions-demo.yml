name: Daily News Job
on:
  schedule:
    - cron: "0 9 * * *"   # Fixed cron expression
  workflow_dispatch:
jobs:
  run-news-script:
    runs-on: ubuntu-latest
    services:
      postgres:
        # ... your postgres config
    steps:
      # ... your steps
      - name: Create .env file
        run: echo "${{ secrets.CONFIG }}" > .env  # This pulls from repo secrets
      - name: Run news script
        run: python scripts/load_to_db.py